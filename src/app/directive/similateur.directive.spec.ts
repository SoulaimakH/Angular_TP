import { SimilateurDirective } from './similateur.directive';

describe('SimilateurDirective', () => {
  it('should create an instance', () => {
    const directive = new SimilateurDirective();
    expect(directive).toBeTruthy();
  });
});
